# version: "3.8"

services:
  web:
    build: .
    command: npm run dev
    container_name: justflix-app
    ports:
      - "${PORT}:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - .env
